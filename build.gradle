plugins {
    id('java-library')
}

repositories { mavenCentral() }
dependencies { implementation 'org.javacord:javacord:3.8.0' }

sourceSets {
    main {
        java {
            srcDirs = ['src/main']
        }
    }
}